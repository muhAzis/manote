{
  "buildCommand": "cd manote && npm install && npm run build",
  "outputDirectory": "manote/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.js"
    }
  ]
}
